## Go To Chain

- We'll need to deploy `SimpleTrader.sol` that will be the taker smart account through which we will be swapping
- We'll need to deploy `XYCSwapper.sol` that will be our smart contract that will use the strategy and handle swaps from the `SimpleTrader`
- We'll need to deploy three mock ERC20 tokens: mockUSDC, mockUSDT, mockDAI
